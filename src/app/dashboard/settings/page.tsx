import { SectionCard } from "@/components/shared/section-card";

export default function SettingsPage() {
  return <SectionCard title="Settings" subtitle="Control privacy, preferences, and notifications." />;
}
