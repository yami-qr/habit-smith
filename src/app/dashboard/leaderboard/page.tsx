import { SectionCard } from "@/components/shared/section-card";

export default function LeaderboardPage() {
  return <SectionCard title="Leaderboard" subtitle="Track streak rankings and social momentum." />;
}
