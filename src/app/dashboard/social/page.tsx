import { SectionCard } from "@/components/shared/section-card";

export default function SocialPage() {
  return <SectionCard title="Social" subtitle="Stay accountable with your friends and community." />;
}
