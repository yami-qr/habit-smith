import { SectionCard } from "@/components/shared/section-card";

export default function AnalyticsPage() {
  return <SectionCard title="Analytics" subtitle="Visualize completion and consistency metrics." />;
}
